# Snyk (https://snyk.io) policy file, patches or ignores known vulnerabilities.
version: v1.25.0
ignore:
  # Exclude virtual environment from scans
  '**/venv/**':
    - '*'
    reason: 'Virtual environment contains third-party dependencies that are not part of our codebase'
    expires: 2025-12-31T00:00:00.000Z

# Patch specific vulnerabilities if needed
patch:
  # Add patches here if needed in the future
